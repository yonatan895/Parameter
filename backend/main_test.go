package main

import "testing"

// TestDummy exists to ensure the test suite runs even when no other tests are
// defined. Real tests can be added as the project evolves.
func TestDummy(t *testing.T) {}
